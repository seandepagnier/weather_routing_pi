This directory structure is used by the mingw build process script file:
circleci-build-mingw.sh

If new dependencies for the build need to be added then they should go in the file:
mingw/fedora/opencpn-deps.spec

These dependencies can be subtally different from the other spec files being used
